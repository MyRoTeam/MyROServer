HOW TO RUN TESTS:

1. cd into the root directory
2. run "npm install"
2. run "npm install -g mocha"
3. run "npm install should --save-dev"
4. run "npm install supertest --save-dev"
5. run "npm install winston"
6. cd into the unitTests/ directory
7. run "mocha"
